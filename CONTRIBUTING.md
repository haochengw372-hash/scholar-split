# 贡献指南

欢迎提交 Issue 和 Pull Request。提交代码前请：

1. 保持依赖为零，除非新增依赖有明确必要性。
2. 不提交 PDF、翻译结果、API Key、浏览记录或本机绝对路径。
3. 为行为修复补回归测试。
4. 运行 `npm test` 与 `npm run check`。
5. UI 变更附侧栏或双栏阅读页截图。

第三方后端适配必须放在独立仓库或明确的不同许可证目录中，不得把 AGPL 代码重新标记为 MIT。
