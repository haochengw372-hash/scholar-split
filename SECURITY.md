# 安全策略

## 支持范围

仅维护仓库默认分支上的最新版本。

## 报告漏洞

请通过 GitHub Security Advisory 私下报告可能泄露 API Key、任意读取本地文件、目录穿越或跨站权限扩大的漏洞。请勿在公开 Issue 中附带真实密钥、论文全文或个人路径。

## 设计约束

- 本地服务必须绑定回环地址，不应暴露到局域网或公网。
- 扩展不接收 API Key，服务端不得在健康检查、任务记录或错误消息中返回密钥。
- 网站读取权限按来源域名临时申请。
- 不使用远程脚本、`eval`、内容脚本或通配持久网站权限。
